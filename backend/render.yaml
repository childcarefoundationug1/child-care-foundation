services:
  - type: web
    name: child-care-foundation-api
    rootDir: backend
    runtime: node
    buildCommand: npm install
    startCommand: npm start
    envVars:
      - key: NODE_ENV
        value: production
